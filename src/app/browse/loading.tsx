import { BrowseGridSkeleton } from "@/components/browse/browse-skeleton";

/** Route-level loading UI while the browse page shell prepares. */
export default function BrowseLoading() {
  return <BrowseGridSkeleton />;
}
